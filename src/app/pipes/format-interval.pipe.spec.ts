import { FormatIntervalPipe } from './format-interval.pipe';

describe('FormatIntervalPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatIntervalPipe();
    expect(pipe).toBeTruthy();
  });
});
