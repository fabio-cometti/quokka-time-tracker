import { FormatSumPipe } from './format-sum.pipe';

describe('FormatSumPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSumPipe();
    expect(pipe).toBeTruthy();
  });
});
